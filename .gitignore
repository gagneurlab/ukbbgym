# Python environments (see "Environment setup" in README.md)
.venv/
.venv-hail/
__pycache__/
*.py[cod]

# local data / figure output
data/
paper_figures/
figures/

.ipynb_checkpoints/
.DS_Store
configs/_custom_score.yaml

# LaTeX build products (may embed local absolute paths)
*.aux
*.fdb_latexmk
*.fls
*.log
*.synctex.gz

# UKB-Git-Audit-Tool reports (may list candidate participant IDs)
ukb_audit_reports/

# uv lockfile: per-file sizes trip the UKB-Git-Audit-Tool (7-digit "EID" false positives)
uv.lock
